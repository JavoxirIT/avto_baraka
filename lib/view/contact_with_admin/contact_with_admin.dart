import 'package:flutter/material.dart';

class ContactWithAdmin extends StatefulWidget {
  const ContactWithAdmin({Key? key}) : super(key: key);

  @override
  ContactWithAdminState createState() => ContactWithAdminState();
}

class ContactWithAdminState extends State<ContactWithAdmin> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Adminga murat'),
      ),
      body: Container(),
    );
  }
}
